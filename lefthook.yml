pre-commit:
  commands:
    biome:
      glob: "*.{ts,js,json,jsonc,graphql}"
      run: npx biome check --staged --write {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
